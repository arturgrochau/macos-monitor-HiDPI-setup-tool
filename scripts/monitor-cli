#!/usr/bin/env bash
# MIT License
# Copyright (c) 2025 Artur Grochau
# 
# This file is part of Monitor Layout Manager.
# See the LICENSE file in the root directory for full license text.

# Global CLI wrapper for Monitor Setup Tool
# This provides 'monitor-layout' command system-wide

# Find the installation directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
INSTALL_DIR="$(dirname "$SCRIPT_DIR")"

# Execute the unified entry point with CLI mode
exec "$INSTALL_DIR/monitor-layout" --cli "$@"
    
    # Find Python executable (prefer venv)

