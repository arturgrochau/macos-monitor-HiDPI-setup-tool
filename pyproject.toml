[project]
name = "monitor-layout-manager"
version = "1.5.0"
description = "The easiest way to manage multiple monitors on macOS."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Artur Grochau", email = "arturgrochau@gmail.com" }]
